//
// Created by icekylin on 12/10/2022.
//

/*
 * # 逻辑运算符
 *
 * - 用于连接多个条件（一般来讲就是关系表达式）
 * - 最终的结果要么是真（非 0 表示），要么是假（0 表示）
 * - 在进行 && 操作时，若第一个条件为 False，则后面的条件不再判断
 * - 在进行 || 操作时，若第一个条件为 True，则后面的条件不再判断
 */
int main() {
    return 0;
}
